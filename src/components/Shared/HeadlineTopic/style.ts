import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  headlineContainer: {
    alignItems: 'center',
  },
  headlineText: {
    fontWeight: '700',
    textAlign: 'center',
  },
});
