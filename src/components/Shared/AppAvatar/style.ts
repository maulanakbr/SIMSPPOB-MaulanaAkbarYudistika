import {StyleSheet} from 'react-native';

import theme from '@/theme';

export default StyleSheet.create({
  avatarContainer: {
    position: 'relative',
  },
  avatar: {
    alignSelf: 'center',
    borderRadius: 80,
    height: 150,
    width: 150,
  },
});
